
generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}

model Casa {
id  String @id 
name  String
descricao String
foto  String
preco String

}